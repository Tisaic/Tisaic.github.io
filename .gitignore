# Dev-only test harness artifacts (never shipped by Pages).
test/node_modules/
test/screenshots/
test/package-lock.json
# Throwaway analysis/capture scripts (dev tooling, not shipped).
scratchpad/

# scratch: cached simulation records for the offline experiments
.dye-cache/
